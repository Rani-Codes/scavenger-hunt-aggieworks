/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      '127.0.0.1', 
      'localhost', 
      'scavenger-hunt-aggieworks.onrender.com'
    ],
  },
};

export default nextConfig;
